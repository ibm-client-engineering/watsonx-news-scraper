import HomepageTemplate from '../components/Homepage.js';
export default HomepageTemplate;

## Automatic News Analysis using Watson<span style="color:blue">x</span>

<PageDescription>

This is a webscraper that uses watson<span style="color:blue">x</span>.ai to analyze news of financial markets from around the world. Currently it uses CNN and CNBC news articles, which are then analyzed for sentiment using watsonx.ai.

</PageDescription>

